rootProject.name = "git-submodule-plugin"

// Plugin management (optional but recommended)
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

// Dependency resolution management (optional)
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}